import { defineConfig } from "vite";import react from "@vitejs/plugin-react";import { writeFileSync, mkdirSync } from "node:fs";import { resolve } from "node:path";const BT_NGINX_LINES = ["# ===== 宝塔 Nginx 配置:粘贴到 网站 -> 配置文件 -> server { ... } 内 =====","location / {","    try_files $uri $uri/ /index.html;","}","","location ~* \\.(?:js|css|woff2|svg|png|jpg|jpeg|webp|mp4)$ {","    expires 30d;",`add_header Cache-Control "public, max-age=2592000, immutable";`,`add_header X-Content-Type-Options "nosniff";`,"    access_log off;","}","","location = /index.html {",`add_header Cache-Control "no-cache, must-revalidate";`,`add_header X-Frame-Options "SAMEORIGIN";`,`add_header X-Content-Type-Options "nosniff";`,`add_header Referrer-Policy "strict-origin-when-cross-origin";`,`add_header Permissions-Policy \"camera=(), microphone=(), geolocation=()\";`,"}","","location /media/ {","    expires 7d;",`add_header Cache-Control "public, max-age=604800, must-revalidate";`,`add_header X-Content-Type-Options "nosniff";`,"}","","location ~* \\.(?:woff2|ttf|otf)$ {",`add_header Access-Control-Allow-Origin "*";`,"}",""];
const DEPLOY_LINES = ["# 宝塔部署(zsl.codebn.cn)","","## 1. 上传 dist/ 内全部内容到网站根目录","保留带 hash 的文件名(assets/index-D15chkaL.js 等不要改)。","","## 2. 在 宝塔 -> 网站 -> 配置文件 -> server { ... } 块里粘贴 bt-nginx.conf 全部内容","保存即生效。","","## 3. 验证清单","- [ ] 打开 https://zsl.codebn.cn ,Hero 视频自动播放","- [ ] 9 个 app 卡片点击跳到正确地址","- [ ] 刷新任意子路径不报 404(SPA fallback)","- [ ] 浏览器标签页标题是「森林 · Senlin Studio | 教学现场」","","## 4. 已知小问题","- typing 链接 token 已过期(2025-11-25),后端换 token 即可",""];
const writeBtArtifacts = () => ({name: "bt-deploy-artifacts",closeBundle() {const dist = resolve(process.cwd(), "dist");mkdirSync(dist, { recursive: true });writeFileSync(resolve(dist, "bt-nginx.conf"), BT_NGINX_LINES.join("\n"));writeFileSync(resolve(dist, "DEPLOY.md"), DEPLOY_LINES.join("\n"));},});
export default defineConfig({server: { headers: { "X-Content-Type-Options": "nosniff" } },preview: { headers: { "X-Content-Type-Options": "nosniff" } },plugins: [react(), writeBtArtifacts()],build: {target: "es2020",cssCodeSplit: true,cssMinify: "lightningcss",modulePreload: { polyfill: false },rollupOptions: {output: {manualChunks(id) {if (!id.includes("node_modules")) return;if (id.includes("framer-motion")) return "vendor-motion";if (id.includes("@use-gesture")) return "vendor-gesture";if (id.includes("lucide-react")) return "vendor-icons";return "vendor";},},},},});

